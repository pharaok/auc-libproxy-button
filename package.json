{
  "name": "auc-library-proxy-button",
  "description": "Adds a button to open page through the AUC Library proxy.",
  "scripts": {
    "firefox:deploy": "pnpm --filter extension build:firefox && cp apps/extension/.output/firefox-mv2/firefox-mv2.xpi apps/web/static/"
  }
}
